#!/usr/bin/env bash
# Double-clickable launcher (macOS Finder) — same as running ./start.
# First double-click may need: right-click → Open, to get past Gatekeeper.
cd "$(dirname "$0")" && exec ./start
